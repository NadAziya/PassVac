import React from "react";
import { View, ScrollView, Text } from "react-native";

const Vaccin = (props) => {
  return (
    <ScrollView>
      <Text>Screen pour les types du vaccin dz</Text>
    </ScrollView>
  );
};

export default Vaccin;
